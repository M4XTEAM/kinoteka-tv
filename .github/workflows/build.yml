name: build-ipa
on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: macos-26
    steps:
      - uses: actions/checkout@v4

      - name: Show Xcode
        run: xcodebuild -version

      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Generate Xcode project
        run: xcodegen generate

      - name: Build (unsigned)
        run: |
          set -o pipefail
          xcodebuild \
            -project KinotekaTV.xcodeproj \
            -scheme KinotekaTV \
            -configuration Release \
            -sdk iphoneos \
            -derivedDataPath build \
            CODE_SIGNING_ALLOWED=NO CODE_SIGN_IDENTITY="" CODE_SIGNING_REQUIRED=NO \
            ONLY_ACTIVE_ARCH=NO \
            build

      - name: Package unsigned IPA
        run: |
          APP=$(find build/Build/Products/Release-iphoneos -maxdepth 1 -name "*.app" | head -1)
          echo "app: $APP"
          rm -rf Payload && mkdir Payload
          cp -R "$APP" Payload/
          zip -qry KinotekaTV-unsigned.ipa Payload
          ls -la KinotekaTV-unsigned.ipa

      - uses: actions/upload-artifact@v4
        with:
          name: KinotekaTV-unsigned-ipa
          path: KinotekaTV-unsigned.ipa
          if-no-files-found: error

      - name: Publish Release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          gh release delete ipa-latest -y --cleanup-tag 2>/dev/null || true
          gh release create ipa-latest KinotekaTV-unsigned.ipa \
            --title "KinotekaTV unsigned IPA" \
            --notes "Автосборка ${GITHUB_SHA}" \
            --target "$GITHUB_SHA"

      - name: Commit IPA to ipa-bin branch (git-доставка)
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          git checkout --orphan ipa-bin
          git reset
          echo "IPA build of ${GITHUB_SHA}" > IPA_README.txt
          git add -f KinotekaTV-unsigned.ipa IPA_README.txt
          git -c user.email=ci@kinoteka -c user.name=CI commit -m "ipa build ${GITHUB_SHA}"
          git push -f "https://x-access-token:${GH_TOKEN}@github.com/${GITHUB_REPOSITORY}.git" ipa-bin
